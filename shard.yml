name: svd
version: 0.1.0

authors:
  - Stephanie Wilde-Hobbs <git@stephanie.is>

targets:
  crystal-svd:
    main: src/main.cr

scripts:
  postinstall: shards build

executables:
  - crystal-svd

crystal: '>= 1.13.0'

license: MIT
